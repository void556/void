name: Build PySide6 Android APK

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Create Virtual Environment and Install Dependencies
        run: |
          python -m venv venv
          ./venv/bin/python -m pip install --upgrade pip
          ./venv/bin/pip install pyside6 jinja2 pkginfo tqdm "packaging==24.1"

      - name: Initialize Android Build Environment
        run: |
          ./venv/bin/pyside6-android-deploy --init || true

      - name: Build APK with pyside6-android-deploy
        run: |
          ./venv/bin/pyside6-android-deploy --config-file pysidedeploy.spec

      - name: List files for debugging
        run: |
          find . -name "*.apk"

      - name: Find and Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: void-app-apk
          path: |
            bin/*.apk
            **/*.apk
            dist/*.apk
